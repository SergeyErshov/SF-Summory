apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp-ingress
spec:
  ingressClassName: nginx
  defaultBackend:
    service:
      name: service-django
      port:
        number: 3003